# IU Work Tracker — Agent Instructions

Before making changes:

1. Read `docs/AI_HANDOFF.md`.
2. Run `git status`.
3. Inspect recent Git history.
4. Read only the authoritative documents relevant to the requested task.
5. Preserve the existing architecture unless explicitly instructed otherwise.

IMPORTANT:

- Never assume SharePoint is the active persistence provider.
- DEV SharePoint has been provisioned and verified.
- Application persistence currently remains on the prototype provider.
- Do not begin the next development phase without explicit instruction.
- Do not expand scope.
- Prefer the smallest safe patch.
- Claude Code and Codex must not edit the repository simultaneously.

At completion, update `docs/AI_HANDOFF.md` only if project state materially changed.
